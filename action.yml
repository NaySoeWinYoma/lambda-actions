name: 'Lambda Deploy'
description: 'Deploy app on Lyrid'

inputs:
  functionFolder:  
    description: 'Lambda Function Folder Name'
    default: "functions"
    required: true
runs:
  using: composite
  steps:
    - run: ls -la
      shell: bash
    - run: sudo bash go run ${{ github.action_path }}/main.go
      shell: bash
